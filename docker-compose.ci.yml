# Local Jenkins + SonarQube, for exercising the CI pipeline in Jenkinsfile against a real
# SonarQube quality gate before either is provisioned for real (specs/012-sonarqube-quality-gate).
#
# Separate project name and compose file from docker-compose.yml (the application stack) — these
# are CI tooling, not part of the platform being built, and should be started/stopped independently.
#
#   docker compose -f docker-compose.ci.yml up -d
#   docker compose -f docker-compose.ci.yml down        # stop, keep data
#   docker compose -f docker-compose.ci.yml down -v     # stop and wipe Jenkins/SonarQube state
name: ecomerce-ci

networks:
  ci-backbone:
    driver: bridge

volumes:
  jenkins-data:
  sonarqube-data:
  sonarqube-extensions:
  sonarqube-logs:

services:
  jenkins:
    # Built, not pulled: the Jenkinsfile's agent requirements (.NET 10 SDK, Node 22 + pnpm, the
    # Docker CLI, git) previously existed only as a manually-built, untagged local image — silently
    # lost ("dotnet: not found") the moment the container was recreated from a plain
    # `jenkins/jenkins:lts-jdk17`. See docker/ci/jenkins.Dockerfile for how it was reconstructed.
    build:
      context: .
      dockerfile: docker/ci/jenkins.Dockerfile
    image: ecomerce-ci-jenkins:local
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins-data:/var/jenkins_home
      # Groovy init scripts run on every controller start, so the SonarQube server connection
      # survives `down -v` instead of having to be re-entered through the UI (T013).
      - ./docker/ci/jenkins-init:/var/jenkins_home/init.groovy.d:ro
      # Real tokens, gitignored. Mounted read-only so the init script can read them and nothing
      # in a build can write them.
      - ./.ci-secrets:/run/ci-secrets:ro
    networks: [ci-backbone]
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/login || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  sonarqube:
    image: sonarqube:community
    ports:
      - "9000:9000"
    volumes:
      - sonarqube-data:/opt/sonarqube/data
      - sonarqube-extensions:/opt/sonarqube/extensions
      - sonarqube-logs:/opt/sonarqube/logs
    networks: [ci-backbone]
    environment:
      # Skips the Elasticsearch vm.max_map_count host-kernel check, which normally requires a host
      # sysctl change (262144) not worth making just to run a local/dev SonarQube in Docker Desktop.
      # Do not carry this into a real deployment.
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
      # Required by the SonarQube Community Branch Plugin (research.md Decision 8): without this
      # Java agent, the plugin's ServerExtensionInstaller refuses to load ("did not detect agent
      # modifications") and the whole server aborts startup — this is not just a missing-feature
      # case, it is a hard crash. This was previously set out-of-band (never persisted here), so a
      # plain `docker compose down`/`up` silently dropped it and broke SonarQube entirely. The path
      # is relative to SONARQUBE_HOME and must match the jar's actual filename/version in the
      # sonarqube-extensions volume (see `docker run --rm -v ecomerce-ci_sonarqube-extensions:/data
      # busybox ls /data/plugins/` to check after a plugin upgrade).
      # The plugin instruments each SonarQube JVM component separately — web alone is not enough;
      # Compute Engine (which actually processes analysis reports and computes gate results) needs
      # its own agent too, or the server aborts startup the same way for that component instead.
      SONAR_WEB_JAVAADDITIONALOPTS: "-javaagent:./extensions/plugins/sonarqube-community-branch-plugin-26.5.0.jar=web"
      SONAR_CE_JAVAADDITIONALOPTS: "-javaagent:./extensions/plugins/sonarqube-community-branch-plugin-26.5.0.jar=ce"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
