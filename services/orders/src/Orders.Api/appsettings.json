{
  "ConnectionStrings": {
    "OrdersDb": "Server=orders-db;Database=orders;TrustServerCertificate=True"
  },
  "Identity": {
    "Authority": "http://identity-api:8080",
    "Audience": "ecommerce-api"
  },
  "//FeatureToggles": "015-deny-by-default-authz, research.md Decision 5 / constitution Principle X. Off by default — the safe rollback state — until confirmed stable.",
  "FeatureToggles": {
    "AuthorizationRequireApiScope": false
  },
  "//Chaos": "025-chaos-pod-kill-latency, research.md Quyết định 1 / constitution Principle X. Off by default — a request cannot turn this on by itself even when true. Not a rollout toggle with a removal date: a permanent SRE chaos-exercise tool, only ever true on a diagnostic cluster, never in a configuration that represents production.",
  "Chaos": {
    "AllowLatencyInjection": false
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
