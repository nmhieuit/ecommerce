# Workspace packages resolve to the local copy rather than the registry, so `@ecommerce/api-client`
# in apps/web is always the one in this repository.
link-workspace-packages=true
prefer-workspace-packages=true
